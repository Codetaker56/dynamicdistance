{
  "schemaVersion": 1,
  "id": "dynamicdistance",
  "version": "${version}",
  "name": "DynamicDistance",
  "description": "Per-player view, chunk-send and simulation distance. Each player gets exactly what their client asks for (clamped to a configurable window), no client mod required.",
  "authors": ["Codetaker56"],
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "main": ["io.github.codetaker56.dynamicdistance.DynamicDistance"]
  },
  "mixins": [
    "dynamicdistance.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": ">=26.1 <27",
    "java": ">=25",
    "fabric-api": "*"
  }
}
